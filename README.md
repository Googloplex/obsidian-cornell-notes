# Cornell Notes — Obsidian Plugin

Плагин для [Obsidian](https://obsidian.md), реализующий метод конспектирования **Cornell Notes** — трёхпанельный редактор с разделами «Вопросы / Идеи», «Конспект» и «Резюме».

Каждая секция хранится как отдельный `.md` файл — полная поддержка wikilinks, embeds, LaTeX, backlinks, graph view и поиска.

## Возможности

- **Трёхпанельный редактор** — locked layout с секциями: вопросы (слева), конспект (справа), резюме (снизу)
- **Секции = реальные .md файлы** — `имя_cues.md`, `имя_notes.md`, `имя_summary.md` в подпапке
- **Live Preview редактирование** — встроенный CodeMirror 6 (как в Canvas) с рендерингом wikilinks, embeds, LaTeX прямо при вводе — без переключения режимов
- **`.cornell` манифест** — JSON-файл с метаданными (заголовок, теги, даты), открывает 3-панельный view
- **Перетаскиваемые разделители** — вертикальный между cues/notes, горизонтальный для resize summary
- **Корневая папка конспектов** — автосоздание при старте, путь настраивается (по умолчанию `Cornell Notes/`)
- **Агрегация / экспорт** — собирает содержимое всех 3 файлов в единый Markdown
- **Контекстное меню** — создание конспекта из папки в проводнике
- **Иконка в ленте** для быстрого создания
- **Навигация Tab** между панелями, **Shift+Tab** для вставки табуляции
- **Подсчёт слов**, метки времени создания/изменения
- **Теги** для организации конспектов
- **Адаптивная вёрстка** — поддержка мобильных устройств
- **Поддержка печати**

## Структура конспекта

При создании конспекта «Лекция по физике» в папке `Notes/`:

```
Notes/
  Лекция по физике/
    Лекция по физике.cornell   ← манифест (открывает view)
    Лекция по физике_cues.md   ← вопросы / идеи
    Лекция по физике_notes.md  ← основной конспект
    Лекция по физике_summary.md ← резюме
```

Каждый `.md` файл содержит YAML frontmatter с типом секции и ссылкой на конспект.

## Установка

### Ручная установка

1. Скачайте `main.js`, `manifest.json` и `styles.css` из релиза
2. Создайте папку `cornell-notes` в `<vault>/.obsidian/plugins/`
3. Скопируйте файлы в созданную папку
4. Включите плагин в настройках Obsidian → Community plugins

### Сборка из исходников

```bash
git clone https://github.com/Googloplex/obsidian-cornell-notes.git
cd obsidian-cornell-notes
npm install
npm run build
```

Для режима разработки с автоматической пересборкой:

```bash
npm run dev
```

## Использование

1. Нажмите иконку 📋 в ленте или выполните команду `Создать новый конспект (Cornell Notes)`
2. Укажите название и папку
3. Панели содержат встроенные Live Preview редакторы — пишите сразу с рендерингом. Tab для навигации между панелями, Shift+Tab для вставки табуляции, Escape для снятия фокуса
4. Для экспорта: команда `Экспортировать конспект в Markdown` — собирает все секции в один документ

## Структура проекта

```
├── main.ts              # Точка входа: регистрация view, команд, создание структуры
├── src/
│   ├── types.ts             # Манифест, константы секций, frontmatter, утилиты
│   ├── embedded-editor.ts   # Встроенный CM6 Live Preview редактор (фабрика)
│   ├── view.ts              # 3-панельный view: embedded CM6 editors
│   ├── modals.ts            # Создание конспекта, агрегация/экспорт
│   └── settings.ts          # Настройки плагина
├── styles.css               # Стили (CM6 editor, layout, responsive, print)
├── manifest.json        # Метаданные плагина для Obsidian
├── package.json         # Конфигурация npm
└── tsconfig.json        # Конфигурация TypeScript
```

## Метод Cornell Notes

Метод конспектирования Корнелла разработан в 1950-х годах в Корнеллском университете. Страница делится на три зоны:

| Зона | Назначение |
|------|-----------|
| **Вопросы / Идеи** | Ключевые слова, вопросы к материалу, связи с другими темами |
| **Конспект** | Основные записи во время лекции или чтения |
| **Резюме** | Краткий итог — заполняется после занятия для закрепления |

## Лицензия

MIT
